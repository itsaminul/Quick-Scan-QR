import 'package:flutter/material.dart';

class AppColors {
  // Primary brand color (Clean blue – modern & trusted)
  static const Color primary = Color(0xFF2563EB);

  // Light mode background (Soft neutral – glass UI friendly)
  static const Color background = Color(0xFFF2F4F8);

  // Dark mode background (Deep navy – premium dark)
  static const Color darkBg = Color(0xFF0F172A);

  // Dark text (Readable & soft, not pure black)
  static const Color textDark = Color(0xFF020617);

  // Light text
  static const Color textLight = Color(0xFFFFFFFF);
}
